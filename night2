<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>動画視聴ページへリダイレクト中</title>
    <style>
        body {
            font-family: sans-serif;
            text-align: center;
            padding: 50px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        .container {
            max-width: 500px;
            margin: 0 auto;
            background: rgba(255,255,255,0.1);
            padding: 40px;
            border-radius: 20px;
            backdrop-filter: blur(10px);
        }
        .countdown {
            font-size: 48px;
            font-weight: bold;
            margin: 30px 0;
            color: #FFD700;
        }
        .manual-link {
            display: inline-block;
            background: white;
            color: #333;
            padding: 15px 30px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: bold;
            margin-top: 30px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🎬 動画視聴ページへ</h1>
        <p>TikTok Liteで「探偵ナイトスクープ」を視聴します</p>
        
        <div class="countdown" id="countdown">3</div>
        
        <p>自動的に移動しない場合は、以下のリンクをタップしてください：</p>
        
        <a href="https://www.tiktok.com/lite/invite/ZSHovTGXACKeR-94XGE" 
           class="manual-link" id="manualLink">
            📱 TikTok Liteで視聴する
        </a>
        
        <p style="margin-top: 30px; font-size: 14px; opacity: 0.8;">
            ※ 初回のみアプリの確認画面が表示される場合があります
        </p>
    </div>

    <script>
        // 招待コード
        const inviteCode = "ZSHovTGXACKeR-94XGE";
        
        // リダイレクト先URL
        const redirectUrl = `https://www.tiktok.com/lite/invite/${inviteCode}`;
        
        // カウントダウン処理
        let count = 3;
        const countdownElement = document.getElementById('countdown');
        const manualLink = document.getElementById('manualLink');
        
        const countdownInterval = setInterval(() => {
            count--;
            countdownElement.textContent = count;
            
            if (count <= 0) {
                clearInterval(countdownInterval);
                redirectToTikTok();
            }
        }, 1000);
        
        // リダイレクト関数
        function redirectToTikTok() {
            // GitHub Pagesでは直接的なリダイレクトのみ可能
            window.location.href = redirectUrl;
        }
        
        // 手動リンクも同じURLに
        manualLink.href = redirectUrl;
        
        // バックアップ：10秒後に強制リダイレクト
        setTimeout(() => {
            if (!document.hidden) {
                redirectToTikTok();
            }
        }, 10000);
    </script>
</body>
</html>
